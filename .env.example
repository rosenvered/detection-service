# Copy to .env and set your key from the take-home instructions:
#   cp .env.example .env
#
# Load before starting the server:
#   set -a && source .env && set +a && go run ./cmd/server

OPENAI_API_KEY=
ADDR=:8080
DB_PATH=detection.db
